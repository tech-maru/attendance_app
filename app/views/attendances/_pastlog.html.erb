<div class="modal-dialog">
  <!-- モーダルコンテンツ:モーダルに表示させる内容-->
  <div class="modal-content">
    <!-- &timesは「×ボタン」の表示-->
    <div class="modal-body">
      <button type="button" class="close" data-dismiss="modal">&times;</button>
      <h4 id="hoge">勤怠ログ</h4>
      <div class = "form">
        <%= form_with(scope: :date, url: attendances_past_log_user_path, method: :get, id: "date_form") do |f| %> 
        <%= f.datetime_select :date, {start_year: 2019, discard_day: true, discard_hour: true, discard_minute: true, include_blank: true} %>
        <% end %>
      </div>
      <table class="table table-bordered">
  <thead>
    <tr>
      <th>日付</th>
      <th>変更前出社時間</th>
      <th>変更前退社時間</th>
      <th>変更後出社</th>
      <th>変更後退社</th>
      <th>指示者</th>
      <th>承認日</th>
    </tr>
  </thead>
</table>
</table>
    </div>
  </div>
</div>
<script>
$(function(){
  $("#date_date_2i").change(function(){
    $("#date_form").submit();
  });
});
</script>

