<%= form_with(model: basis, local: true) do |form| %>
  <% if basis.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(basis.errors.count, "error") %> prohibited this basis from being saved:</h2>

      <ul>
      <% basis.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :basis_name %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type, id: :basis_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
